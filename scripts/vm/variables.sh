APP_NAME=$APP_NAME
APP_ENV=$APP_ENV
APP_KEY=$APP_KEY
APP_DEBUG=$APP_DEBUG
APP_URL=$APP_URL
AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY
AWS_DEFAULT_REGION=$AWS_DEFAULT_REGION
AWS_BUCKET=$AWS_BUCKET
BROADCAST_DRIVER=$BROADCAST_DRIVER
CACHE_DRIVER=$CACHE_DRIVER
DB_CONNECTION=$DB_CONNECTION
DB_HOST=$DB_HOST
DB_PORT=$DB_PORT
DB_DATABASE=$DB_DATABASE
DB_USERNAME=$DB_USERNAME
DB_PASSWORD=$DB_PASSWORD
EB_ENVIRONMENT_NAME=$EB_ENVIRONMENT_NAME
ENVIRONMENT_NAME_PRODUCTION=$ENVIRONMENT_NAME_PRODUCTION
ENVIRONMENT_NAME_STAGING=$ENVIRONMENT_NAME_STAGING
FILESYSTEM_DRIVER=$FILESYSTEM_DRIVER
FIREWALL_ENABLED=$FIREWALL_ENABLED
GOOGLE_CLIENT_ID=$GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=$GOOGLE_CLIENT_SECRET
GOOGLE_URL=$GOOGLE_URL
HOST_PRODUCTION=$HOST_PRODUCTION
LOG_CHANNEL=$LOG_CHANNEL
MAIL_MAILER=$MAIL_MAILER
MAIL_HOST=$MAIL_HOST
MAIL_PORT=$MAIL_PORT
MAIL_USERNAME=$MAIL_USERNAME
MAIL_PASSWORD=$MAIL_PASSWORD
MAIL_ENCRYPTION=$MAIL_ENCRYPTION
MAIL_FROM_ADDRESS=$MAIL_FROM_ADDRESS
MAIL_FROM_NAME=$MAIL_FROM_NAME
QUEUE_CONNECTION=$QUEUE_CONNECTION
QUEUE_DRIVER=$QUEUE_DRIVER
REDIS_HOST=$($REDIS_HOST:-127.0.0.1)
REDIS_PASSWORD=$REDIS_PASSWORD
REDIS_PORT=$($REDIS_PORT:-6379)
SESSION_DRIVER=$SESSION_DRIVER
SESSION_LIFETIME=$SESSION_LIFETIME
SES_KEY=$(if [ -z "$SES_KEY" ]; then echo "$AWS_ACCESS_KEY_ID"; else echo "$SES_KEY"; fi)
SES_KEY_SECRET=$(if [ -z "$SES_KEY_SECRET" ]; then echo "$AWS_ACCESS_KEY_SECRET"; else echo "$SES_KEY_SECRET"; fi)
SES_REGION=$(if [ -z "$SES_REGION" ]; then echo "$AWS_DEFAULT_REGION"; else echo "$SES_REGION"; fi)
SUPERVISE=$SUPERVISE
PUSHER_APP_ID=$PUSHER_APP_ID
PUSHER_APP_KEY=$PUSHER_APP_KEY
PUSHER_APP_SECRET=$PUSHER_APP_SECRET
PUSHER_APP_CLUSTER=$PUSHER_APP_CLUSTER
MIX_PUSHER_APP_KEY=$MIX_PUSHER_APP_KEY
MIX_PUSHER_APP_CLUSTER=$MIX_PUSHER_APP_CLUSTER
